# Squid 机器人 传感器诊断报告

- 日期：2026-06-09
- 串口：COM8 @115200，ESP32-S3
- 诊断方式：独立诊断 sketch（`Squid-Robot-main/Diagnostics/SensorDiag1~9`），未改动主固件

---

## 一、最终结论（一览）

| 传感器 | 接口 | 状态 | 根本原因 | 处理 |
|--------|------|------|----------|------|
| **IMU** EBIMU-9DOFV6 | CH9434A UART3 | ✅ 已修复 | **TX/RX 接反**（引脚定义与超声波相反） | 已对调信号线，正常输出姿态 |
| **深度** MS5837 | ESP32 I2C (IO4/IO5, 0x76) | ❌ 待换新 | **5V 过压烧坏**（MS5837 max VDD≈3.6V） | 换新传感器 + 3.3V 供电 |
| **超声波 前** DYP-L08 | CH9434A UART1 | ✅ 在线有回应 | —（水下专用，岸上无有效距离） | 下水后正常测距 |
| **超声波 左** DYP-L08 | CH9434A UART2 | ✅ 在线有回应 | 同上 | 同上 |
| **超声波 右** DYP-L08 | CH9434A UART0 | ✅ 在线有回应 | 同上 | 同上 |
| **CH9434A** 扩展芯片 | ESP32 SPI(IO10-13) | ✅ 正常 | —（一度误判，实为下游传感器问题） | 无需处理 |
| WiFi/SD/HC-12/Minima/电池 | — | ✅ 正常 | — | — |

**待办：仅需更换深度传感器 MS5837，其余硬件均已确认正常。所有问题均为硬件/接线，主固件无需修改。**

---

## 二、根本原因与修复详情

### 1. IMU —— TX/RX 接反
- 现象：CH9434A U3 RX 只收到零星噪声字节（0x00/0x80），IMU 不回 `<ver>`。
- 根因：**EBIMU 引脚 3=TX / 4=RX，而 DYP 超声波 3=RX / 4=TX，两者完全相反**。
  按超声波的习惯接 IMU 就把 TX/RX 接反了 → CH9434A 的 RX 接到 IMU 的 RX(输入)悬空。
- 修复：对调 IMU 的两根信号线。
- 验证：115200 下稳定输出 `*roll,pitch,yaw`，约 100 帧/秒。
  实测样本：`*140.61,-3.68,72.86`

### 2. 深度 MS5837 —— 5V 过压损坏
- 现象：I2C 扫描 `probe 0x76 err=2`（地址 NACK，总线正常但器件无应答）。
- 根因：传感器被 **5V** 供电，但 MS5837 VDD 绝对最大约 3.6V → 过压损坏。
- 处理：供电已改 3.3V（接线正确），但芯片已损坏，换 3.3V 后仍 `err=2` → 需更换传感器。
- 注意：**MS5837 绝不能接 5V。** 换新后用 SensorDiag8 复测，正常应为 `err=0 ACK` + 读出 ~1013mbar。

### 3. 超声波 ×3 —— 正常（水下专用）
- DYP-L08 是水下超声波，发 0x00 触发后回 4 字节帧 `[FF, H, L, SUM]`。
- 岸上无水介质 → 回 `FF FF FD FB`（校验正确，距离=超量程/无回波）。
- 三路均能收到合法校验帧 → 三个传感器都在线、接线正确、通信正常。
- 岸上响应率偏低（触发46次回6~10次）属正常，下水后稳定连续。

### 4. CH9434A —— 正常（曾误判）
- SPI 寄存器写回读 **48/48 全 OK**，LSR=0x60（健康空闲值）。
- 实际收到超声波合法帧 + IMU 姿态 → 芯片+SPI 完全正常。
- 自检中的"loopback FAIL"是 WCH 芯片内部自环不兼容标准 16550 的误报，非故障。

---

## 三、关键原始数据

### IMU 修复后（SensorDiag8，115200）
```
115200: *帧=100/秒
[IMU] samples=99   当前姿态: *140.61,-3.68,72.86
[IMU] samples=199  当前姿态: *140.61,-3.68,72.87
[IMU] samples=999  当前姿态: *140.61,-3.68,72.86
```

### 深度 MS5837（SensorDiag8，换3.3V后仍故障）
```
[深度] I2C 扫描 SDA=IO4 SCL=IO5 @100kHz:
    found=0 | probe 0x76 err=2 | 0x77 err=2
    （err=2=地址NACK：总线正常，器件无应答/损坏）
    reset 0x76 -> err=4
```

### 三路超声波在线确认（SensorDiag9，岸上）
```
前U1: 触发46 响应6  合法帧6 | 末帧: FF FF FD FB  -> 超量程/无回波(空气正常)
左U2: 触发46 响应10 合法帧9 | 末帧: FF FF FD FB  -> 超量程/无回波(空气正常)
右U0: 触发46 响应9  合法帧9 | 末帧: FF FF FD FB  -> 超量程/无回波(空气正常)
```

### CH9434A 自检（SensorDiag，全4路）
```
UART0~3: LSR=0x60  SPI reg r/w: 48/48 OK
（芯片+SPI 正常）
```

---

## 四、诊断 sketch 清单（位于 Diagnostics/）

| sketch | 用途 |
|--------|------|
| SensorDiag  | I2C 扫描 + CH9434A 自检 + UART 监听 |
| SensorDiag2 | CH9434A 4 路 UART 实时监听（查接反） |
| SensorDiag3 | 超声波触发 + IMU 命令握手 |
| SensorDiag4 | IMU 波特率扫描（`<stop>`） |
| SensorDiag5 | IMU 主动启动逐波特率扫描 |
| SensorDiag6 | IMU `<ver>` 握手定波特率 + 强制 ASCII |
| SensorDiag7 | 修复后验证（深度 + IMU） |
| SensorDiag8 | 深度 I2C 详查(错误码) + IMU 实时姿态 |
| SensorDiag9 | 三路超声波在线/响应统计 |

更换深度传感器后，烧 **SensorDiag8** 即可复测深度+IMU；下水测超声波用 **SensorDiag9**。
（诊断完记得把原主固件烧回：`Diagnostics` 都是独立 sketch，不影响 `ESP32/` 主固件。）

---

## 五、关键经验

1. **IMU 与超声波引脚定义相反**（IMU 3=TX/4=RX，超声波 3=RX/4=TX）——"照同样方式接"会把 IMU 接反。
2. **MS5837 是 3.3V 器件，绝不能接 5V**（绝对最大 ≈3.6V）。
3. 超声波是水下专用，**岸上测不出距离属正常**，下水才有效。
4. 引线接反 / 悬空的 RX 会读到零星噪声字节（0x00/0x80），容易被误判为"有回应"——要以**收到合法/已知格式的帧**为准。
